/**
 * Scoring types (this step) + the ScoreCalculator implementation (step 7):
 * base scoring (광/열끗/띠/피/고도리/홍단/청단/초단), 고 multipliers, 박 multipliers,
 * 멍따, and final settlement — all driven by RuleConfig.
 */
export * from './score-breakdown.js';
export * from './score-engine.js';
