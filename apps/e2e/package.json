{
  "name": "@gostop/e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end integration verification — replay, resume, spectator, state hash, multi-game.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "seeds": "node --import tsx src/print-seeds.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@gostop/backend-game": "workspace:*",
    "@gostop/engine": "workspace:*",
    "@gostop/shared": "workspace:*",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
